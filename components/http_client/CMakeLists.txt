set(COMPONENT_SRCDIRS
    .
)

set(COMPONENT_ADD_INCLUDEDIRS
    .
)

set(COMPONENT_PRIV_REQUIRES
    wifi
    esp_http_client
    esp_https_ota
    config
    mbedtls
)

set(COMPONENT_REQUIRES
)

register_component()
